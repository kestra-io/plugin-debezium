group: io.kestra.plugin.debezium.mongodb
name: "debezium-mongodb"
title: "Debezium MongoDB"
description: "Tasks that stream MongoDB change events into Kestra through Debezium connectors."
body: "Includes Capture plus batch and real-time triggers to ingest MongoDB change data into Kestra flows. Provide a replica set or sharded `connectionString` with credentials, choose snapshotMode, and optionally include or exclude collections; use Trigger to poll in batches or RealtimeTrigger to fire one execution per change."
videos: []
createdBy: "Kestra Core Team"
managedBy: "Kestra Core Team"
