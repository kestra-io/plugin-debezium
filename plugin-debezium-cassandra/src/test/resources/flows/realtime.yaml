id: trigger
namespace: io.kestra.tests

triggers:
  - id: watch
    type: io.kestra.plugin.debezium.cassandra.RealtimeTrigger
    snapshotMode: INITIAL
    hostname: 127.0.0.1
    port: 9042
    username: kestra
    password: passwd

tasks:
  - id: end
    type: io.kestra.core.tasks.debugs.Return
    format: "{{task.id}} > {{taskrun.startDate}}"
